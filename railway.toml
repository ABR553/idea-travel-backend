[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile"

[build.args]
TARGET = "production"

[deploy]
startCommand = "bash entrypoint.prod.sh"
healthcheckPath = "/api/v1/health"
healthcheckTimeout = 30
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
